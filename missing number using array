#include <stdio.h>

int main() {
   int i,k,sum=0,missing;
   scanf("%d",&k);
   int arr[k-1];
   for(i=0;i<k-1;i++)
   {
       scanf("%d",&arr[i]);
       sum=sum+arr[i];
   }
   missing=(k*(k+1))/2-sum;
   printf("%d",missing);
    return 0;
}
